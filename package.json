{
  "name": "escss",
  "version": "1.0.0",
  "description": "ES6 based CSS preprocessor",
  "main": "./src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel --presets es2015,stage-0 -d lib/ escss/",
    "compile2": "babel --presets es2015,stage-0 -d lib/ src/",
    "prerun": "npm run compile && compile2"
  },
  "author": "Justin Sullivan",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-0": "^6.16.0",
    "es-collections": "0.0.2",
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "babel-core": "^6.17.0"
  },
  "bin": {
    "escss": "index.js"
  }
}
